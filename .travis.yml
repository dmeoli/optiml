language: python

python:
  - 3.5
  - 3.6
  - 3.7

install:
  - pip install autograd
  - pip install matplotlib
  - pip install numpy
  - pip install qpsolvers

script:
  - py.test

notifications:
  email: false
