language: python

python:
  - 3.5
  - 3.6
  - 3.7

install:
  - pip install jax
  - pip install jaxlib
  - pip install matplotlib
  - pip install numpy
  - pip install pytest-cov
  - pip install qpsolvers
  - pip install scikit-learn

script:
  - pytest --cov=./
